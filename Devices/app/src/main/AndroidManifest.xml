<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="diakonidze.kartlos.devises" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/icn"
        android:label="@string/app_name"
        android:theme="@style/MyCustomTheme">
        <!--android:uiOptions="splitActionBarWhenNarrow"-->

        <activity
            android:name="diakonidze.kartlos.devises.MainActivity"
            android:label="ჯეოლაბის ტექნიკა"
            android:icon="@drawable/icn"
            android:theme="@style/Theme.AppCompat.Light">
            <!--android:logo="@drawable/logo_geoton"-->
            <!--<meta-data-->
                <!--android:name="android.support.UI_OPTIONS"-->
                <!--android:value="splitActionBarWhenNarrow" />-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Theme.AppCompat.Light"
            android:parentActivityName="diakonidze.kartlos.devises.MainActivity"
            android:name="diakonidze.kartlos.devises.detailspage"
            android:label="დეტალური ინფორმაცია">
        </activity>
    </application>

</manifest>
